# <一句话立意>：<PROJECT> 源码精读课程大纲

> 主教材：<owner>/<repo>（<语言>，<规模，如 90+ crate / 12 万行 TS>）
> 版本锚点：commit `<HASH>`（<DATE>），tag `<TAG>`，本地路径 `<PATH>`
> 对比语料：<项目 A>（<语言>）/ <项目 B>（<语言>）/ <项目 C>
> 性质：研究型书稿 + 互动课页
> 状态：大纲 v1，共 N 章 + M 附录

---

## 课程定位

### 这门课回答哪一个问题

<把整门课收束到一句话。这句话决定哪些内容进、哪些不进。没有它，大纲会退化成源码目录的中文翻译。>

参考两个真实例子：

- DeepSeek Harness：Agent 运行时该长什么样，收束到一条运行时不变量 `Model-visible ⟺ logged`
- OpenAI Codex：当它敢在你机器上跑命令、连网络、带着你的凭据，它凭什么值得被信任，收束到「DSH 让一切可回放，Codex 让一切可拒绝」

### 与姊妹课的关系

<如果是系列第二门以后：说明不重复讲什么、只讲差异做法并链回哪里、哪些是本课独有因此展开讲。>

<章数与主题分组尽量与前一门对齐，便于逐章互链。>

### 读者带走什么

全课累计交付的可迁移物，每条都要具体到能直接用：

- <一套…方案，能直接抄成自己项目的…>
- <一份…决策树>
- <一个…协议规范，可直接复用>
- <一份…成本清单>

---

## 源码语料库

| 用途 | 路径 | 版本锚点 |
|---|---|---|
| 主教材 | `<PATH>` | `<HASH>` |
| 对比语料① | `<PATH>` | `<HASH>` |
| 对比语料② | `<PATH>` | `<HASH>` |
| 检索脚本 | `<PATH>/search.sh` | — |

### 「为什么」的一手材料清单

按优先级列出本项目实际有哪些，并注明哪些是指向外链的空壳、需要跳过：

1. 评审红线：`<AGENTS.md 等>`（<体积>）
2. crate/模块 README：`<列出>`
3. 模块头注释：`<列出写得完整的几个>`
4. 测试文件：`<命名规律>`
5. 官方博客与文档：`<本地归档路径>`

---

## 章节大纲

<每章按下面这个结构写。行号后标 ✓ 表示已实读核实；未标的一律是候选，写作时必须自己定位。>
<注意：✓ 的含义是「曾经核实过」，不是「现在还对」。>

### 第 N 章：<标题>

**核心问题：** <这一章要回答的那一个问题>

**源码入口：**

- `<相对路径>` 第 <A> 至 <B> 行 ✓（<这段是什么>）
- `<相对路径>`（候选，<要找什么>）

**要分析的设计决策：**

- <决策一>
- <决策二>
- <不这样做会出什么事>

**规范性材料：** `<AGENTS.md 第 N 行 / README 某节 / 模块头注释>`

**横向对比对象：** <对比语料某文件>，对比点是 <同一个问题的两种答案>

**演示形态：** 模拟器 / 决策沙盘 / 格式实验台（三选一）

**互链目标：** <姊妹课某页>，或「无直接对照页」

---

## 附录

- 附录 A：<如全部提示词原文索引>
- 附录 B：<如版本差异记录>

---

## 大纲维护记录

| 日期 | 变更 | 原因 |
|---|---|---|
| <DATE> | v1 建立 | — |
